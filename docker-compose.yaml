version: "3"

services:
  postgres:
    image: postgres
    environment:
      - "POSTGRES_USER=example"
      - "POSTGRES_PASSWORD=123"
      - "POSTGRES_DB=example_dev"
    ports:
      - "5432:5432"
